#include <stdio.h>

int main() {
	int x, y, num;
	int lx, ly, rx, ry;

	scanf("%d %d %d", &x, &y, &num);


	for (int i = 0; i < num; i++) {
		scanf("%d %d %d %d", &lx, &ly, &rx, &ry);
	}
	

	return 0;
}