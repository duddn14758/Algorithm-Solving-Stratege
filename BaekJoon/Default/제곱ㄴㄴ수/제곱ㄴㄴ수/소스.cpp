#include <stdio.h>
#include <math.h>

int main() {
	long long min, max;
	long long rmax;
	int loop;

	scanf("%d %d", min, max);
	rmax = sqrt(max);


	for (int i = 2; i < rmax; i++) {
		loop = i;

		while (loop <= max) {

		}
	}

	return 0;
}